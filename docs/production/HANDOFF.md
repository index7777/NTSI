# 《九劫問仙》開發交接

> 修行小遊戲最新狀態先讀：`docs/production/HANDOFF_2026-07-24_MINIGAMES.md`。該文件覆蓋本文件內較早的 ComboCounter、舊 HUD、舊結算與挑水描述。

## 2026-07-24 最新施工方向與跨電腦同步

- 精確順序與驗收以 `docs/production/WORK_QUEUE.md` 的「2026-07-24 分階段施工與同步台帳」為準。
- 順序：挑水正式素材與玩法 → 參悟《清心訣》方向／經脈線校準 → 共用 HUD／結算 → 引氣入體三階段與後續劇情 → 全流程回歸。
- 每完成一階段都要更新兩份交接文件、跑四項驗證、建立提交並推送 `origin/main`；未推送不得標示已同步。
- 挑水現有人物、扁擔與桶是程式幾何原型，必須換成符合本作畫風的正式肩挑動作素材，且維持背景／人物／器具／水／HUD 分層。
- 參悟心法節點必須由上往下；程式路徑必須貼合場景底圖斜線，不得落在線外或倒序。
- 所有日常小遊戲使用同一套 HUD 與共用水墨結算母版；不可略過的劇情小遊戲不得顯示「離開」，也不得由返回或 ESC 繞過。
- 引氣入體固定三段：感應靈氣、引氣入體、氣機相合。施工前必須完整閱讀三段正式規格與現有狀態切換。

## 2026-07-23 S01 最新接手入口

新對話處理 S01 前，必須完整閱讀 `docs/production/S01_HANDOFF_2026-07-23.md`。該文件記錄最新兩張正式母板、人物與場景烘圖規則、返回／齒輪／金圈獨立透明拆層、程式半透明黑底、取名字數規則、使用者手動去背素材歸檔位置、Codex 預備素材保留位置、尚未完成事項與驗證順序。

目前 S01 為「進行中／未驗收」；第二、第三段引氣入體暫停。不得以舊版已通過 build 或舊版尺寸測試視為最新 S01 已完成。

2026-07-23 最新一輪已依使用者即時回饋重做首圖、選角與取名，並通過三尺寸實機、四項驗證及正式 build；詳細尺寸、拆層、選中特效、新齒輪與素材路徑一律以 `docs/production/S01_HANDOFF_2026-07-23.md` 的「最新實裝摘要」為準。狀態仍為進行中／未驗收。

最後更新：2026-07-23

## 2026-07-24 雜役處與問仙石最新修改

- 雜役處新增符合3.5頭身水墨Q版母板的候選 `art/candidates/scenes/chore-office-registration-chibi-v3.png`；窗口人物是不同服制的協助師姐，場景不烘入壞執事。對應七名雜役弟子與一名師姐的可拆分綠幕母板保存在 `art/candidates/characters/chore-office-npcs/chore-office-npc-lineup-chroma-v1.png`，尚未驗收、去背或接入runtime。
- 問仙石水平置中重生成候選為 `art/candidates/scenes/question-stone-centered-generated-v2.png`，尚未經使用者確認，不得覆蓋現行正式背景。
- 雜役處轉場只淡入／淡出「雜役處」三字，移除隊伍說明與「繼續」。標題退場後自動播放壞執事四句正式對話；執事交付心法名稱固定為《清心訣》，不設「收下」按鈕。四句對話結束後強制直接進第一次砍竹，不得先進活動頁或自由探索。
- 心法名稱全案統一為《清心訣》；《納氣訣》是已被最新要求覆蓋的過時名稱，文件、台詞、UI與程式不得再使用。
- 長按問仙石放開滑鼠時，手掌素材必須立即消失；完成蓄力後也不得黏在石面。
- 全部NPC對話預設使用角色同服裝版本的1024×1024透明胸像，放在宣紙對話卡右側；頭像右緣離卡片右緣2個中文字寬，下緣離卡片下緣1個中文字高。只有使用者另行指定時才用全身立繪。
- 問仙石第一次教學與靈根揭示後對話共用 `game/public/assets/characters/portraits/sect-elder-serious-1024-v1.png`；姓名分隔線固定為姓名字數加一個字寬，逐字播放與點擊規則一致。
- 砍竹HUD改用選角共用返回／設定控制；ESC與設定均暫停。修為固定+30，進場顯示已扣除本次後的剩餘修行次數。揮刀不受判定限制，非MISS均播放竹屑斬光；判定圈改為順時針填滿。
- 砍竹結算使用黑色半透明HUD遮罩，統計逐條浮現；顯示基本修為30、額外修為，移除熟練度，按鈕為「再次修行／離開」。
- 新增各小遊戲獨立熟練度存檔 `player.gameProficiency`：每完成一次該玩法增加10%，無上限；當次實得基本修為為 `round(基本修為 × (1 + 熟練度/100))`。舊存檔自動補空物件。例：砍竹基本30、熟練10%時實得33。
- 劇情台詞第一字固定縮排兩個全形中文字；老執事胸像已上移，與右下「（下一句……）」保留垂直間距。
- 砍竹順時針判定圈中心透明，進度只使用金黃色圓弧，不得使用綠色填底或不透明圈素材。
- 小遊戲共用結算宣紙為 `game/public/assets/ui/results/shared-result-panel-v1.png`；水墨內容只含泛用仙山、雲海、遠亭與靈霧，不含竹子、藥草、水桶等玩法專屬物件。使用者提供母版歸檔於 `art/references/gameplay/bamboo/bamboo-result-layout-approved-v1.png`。
- 砍竹空槽底環改用透明美術素材 `game/public/assets/ui/bamboo/timing-empty-ring-v1.png`，Phaser只繪製順時針黃色進度弧。
- 砍竹已接入Kenney Impact Sounds CC0：揮斧、一般木竹命中與PERFECT重擊三層音效。官方原包與License保存在 `art/raw/audio/kenney-impact-sounds/`。
- 小遊戲頂部共用倒數HUD：`3→2→1→開始` 與正式時間倒數使用同一個黑色半透明進度條容器，文字水平及垂直置中，不得另在畫面中央重複倒數。

## 接手必讀：工作主檔與自循環

未完成工作的唯一排序、狀態與驗收閘門記錄於 `docs/production/WORK_QUEUE.md`。新對話、新模型、換電腦、同步或上下文壓縮後，必須先讀 `AGENTS.md`、本文件及 `WORK_QUEUE.md`，再核對 Git、程式與runtime資產；不得只靠對話摘要施工。

使用者在對話中交付的修改、錯誤回報與指定作法一律視為最新正式要求。與舊文件或舊程式衝突時，先更新規格與工作主檔，將舊作法標記過時／暫緩，再施工。每項完成後回寫「已完成／未驗收」並停在驗收閘門；只有使用者驗收後才能標記「已驗收」並進入下一項。退回時改為「退回修改」。

上下文壓縮後必須回讀 `WORK_QUEUE.md`，檢查最早未驗收項目與實際專案；發現漏做就退回補作，後續受影響項目一併複查。除使用者明確插單外，依S00、S01、S02順序完成每個玩家階段的全部工作。

## 接手方式

接手後必須遵守根目錄 `AGENTS.md` 的「工作進度溝通」規範：任何生成、下載、建置、測試或工具等待都要說明原因與下一步；禁止在圖片、彈窗或中間產物後無說明停住。

### 不得自行停工（所有接手模型必讀）

目前總目標是完成2026-07-22與其後交辦的全部素材、實際接入遊戲，並依玩家流程做到筑基九層圓滿；雷劫暫緩。完成首頁、單一場景、素材生成、建置、測試、4174驗收、進度回報或階段交付都只是中間檢查點，不代表可以停止。只要總目標尚未完成且存在安全可執行的下一步，回報後必須立即接續下一項。

只有以下情況可以停：使用者明確要求停止／暫停；必須由使用者做出會實質改變成果的選擇；缺少不可替代素材、付費授權或新權限；工具／環境阻塞且替代方案已嘗試仍失敗。停止時必須主動說明原因、正在等待的項目、已嘗試的替代方案與解除後的下一步，不得只留下成果圖、生成器視窗、測試結果或一句進度回報。

本次已發生的禁止案例：完成首圖右上功能列、建置與4174驗收後，將階段成果回報誤當成回合終點而停止。所有後續模型必須知道這不是技術阻塞，而是錯誤停止判斷；正確行為是立即接續問仙石、執事互動、雜役處及後續玩家流程。

問仙石狀態機已開始重構為「逐句文字→執事對話→長按石頭→轉場」。全案不再使用舊直式整圖裁切或相容備案；問仙石頁已建立原生橫式純背景與獨立中性石頭層。正式完成前仍須補齊面向石頭的玩家側背向素材、獨立執事演出層、接觸手部與白光、四種靈根石色／特效層；每層皆須可單獨隱藏、換色與維護。

問仙石與全案正式 VFX 禁止以 CSS、簡單透明度、Phaser 幾何 Graphics 光圈／線條或少量基本粒子交付；現有程式光圈只屬 debug。正式版必須使用授權清楚的商用特效或生成的專用透明素材，分層實作掌心白光、接觸區光、沿裂紋上行靈流、蓄力、完成爆光、轉場霧光，以及四種靈根各自的石色、紋理光與粒子節奏，再由 Phaser 以遮罩、混合模式、粒子系統、時序與鏡頭組合，並在1200×675實機驗收。

本機採雙埠：4173保留給使用者預覽，Codex自動測試固定優先使用4174，必要時使用4175～4178，不得終止使用者的4173程序。

商業遊戲架構與 demo 後搬遷順序已鎖定於 `docs/production/COMMERCIAL_GAME_ARCHITECTURE.md`。目前可以在單一場景內快速完成 demo，但不得把該結構視為正式架構；demo 驗收後依文件分階段拆分，並為所有小遊戲接入 `runSelfCheck()` 與完整流程回歸。

## 2026-07-23 當前精確檢查點（新對話先讀）

目前最早施工階段仍是 `WORK_QUEUE.md` 的 S04；S01～S03仍為退回修改，但使用者已明確插入S04三個修行原型與S05規格歸檔。不得把插單視為前段已驗收，也不得跳過S04的編譯、實機與素材驗收。

### 程式目前實作狀態

- 技術棧維持 Phaser 3.90 + TypeScript + Vite，原生邏輯座標1200×675，沒有改框架。
- 已新增共用節奏判定核心 `game/src/minigames/shared/RhythmTimingController.ts`。
- 砍竹控制器已改為共用核心的薄封裝；核心使用 `performance.now()` 時間差，不依畫面圓圈尺寸。已修正「一次命中後極快連點被下一輪誤記Miss」：下一輪的 `cycleStartedAt` 到達前拒絕輸入。獨立控制器檢查已確認同幀第二次輸入回傳 `null`、暫停期間不結束、恢復後時間平移、逾時Miss歸零。
- `OpeningScene.ts` 已加入僅在 Vite DEV 生效的測試入口：`?test=bamboo`、`?test=heart-manual`、`?test=water-carry`。正式production build不走測試入口。
- 砍竹30秒原型已有3／2／1、滑鼠／觸控／Space、四段時間判定、連擊、暫停、返回確認、按比例收式及結算；正式揮斧序列、真實竹材音效、正式粒子及首次結算後返回宗門日常地圖仍未完成。
- 2026-07-24 已依 `art/references/gameplay/bamboo-minigame-layout-v1.png` 補入男女各五幀雙手持斧砍竹動作，正式執行幀位於 `game/public/assets/characters/bamboo-actions/`，同框驗收圖為 `art/reviews/bamboo-chop-animation-contact-sheet-v2.png`。舊男女中性站姿仍保留但砍竹玩法不再使用。角色幀已接入 `OpeningScene.playBambooTask()`，成功落斧時播放完整序列。
- 砍竹判定規則最新覆蓋：玩家未操作時，收縮圈抵達完美線即直接重新展開下一輪，不顯示 MISS、不增加 miss、不清空連擊，也不播放揮斧；只有玩家主動在錯誤時機落斧才判 MISS。此規則透過 `passiveResetAtTarget` 僅套用砍竹，不得改壞參悟心法的逾時判定。
- 砍竹差事目標仍為十根，但超額落斧繼續計分與增加修為。左側數量顯示達標前為 `N/10`，超過後固定為 `10/10（超額完成：N）`；統計內仍保存實際總砍伐數。
- 砍竹命中特效已新增正式透明素材 `game/public/assets/vfx/bamboo/bamboo-axe-impact-v1.png`，原稿與處理檔分別保存在 `art/raw/vfx/bamboo/`、`art/processed/vfx/bamboo/`。玩法畫面已縮小左側宣紙面板、調整角色／竹身接觸位置，並把判定圈改為金色完美線、淡綠中心與收縮翠色外圈。
- 參悟心法25秒原型已新增 `game/src/minigames/heart-manual/`，使用相同時間判定核心；中央為氣海→關元→膻中→神庭→百會循環，玩家只點擊時機，不拖曳或點穴位。背景原圖已在 `art/active/scenes/heart-manual-room-landscape-v1.png` 與 runtime。實機已進入畫面，發現左側資訊缺紙張承托、右側安全邊界過近後已修改，但修改後尚未完成第二輪實機驗證。
- 挑水已新增 `game/src/minigames/water-carry/config.ts`、`BalanceController.ts`、`WaterCarryGameController.ts`，並以25秒、A／D／左右鍵、左右觸控區、慣性、隨機外力、左右獨立灑水、持續失衡0.45秒後回彈救回、固定修為+10、單次結算為目標重寫 `playWaterTask()`。目前角色／扁擔／水桶仍是可替換的程式分層原型。
- 挑水明亮無HUD、無角色背景候選已保存為 `art/candidates/scenes/water-carry-bright-landscape-v2.png`；尚未經使用者明確定案、尚未複製到 `game/public/assets`、尚未接入preload，當前挑水程式仍暫用砍竹背景。
- 歷史狀態（已過時）：挑水重構初期曾尚未執行 TypeScript 建置。2026-07-24 最新共用 HUD／倒數／結算整合後已重新執行 `npm run build` 並成功；但瀏覽器實機、觸控與多尺寸驗收仍未完成，不得把「建置通過」誤寫成「玩法已驗收」。

### 唯一安全下一步

1. 先檢查 `git diff`，保留使用者及既有未提交變更。
2. 修正挑水物理中灑水狀態離開危險區時的單次事件重置，檢查 `OpeningScene.ts` 新挑水程式的TypeScript／Phaser API。
3. 使用者若確認明亮候選圖，才將 `water-carry-bright-landscape-v2.png` 升為active/runtime並接入preload；未確認前維持candidate。
4. 執行 `npm run validate:balance`、`npm run validate:landscape`、`npm run validate:assets`、`npm run build`。
5. 以4174；被占用時4175～4178，分別實測 `?test=bamboo`、`?test=heart-manual`、`?test=water-carry`，檢查鍵盤、滑鼠／觸控、暫停恢復、返回不發獎、25／30秒結算、重新開始與640×360橫向。
6. 完成後回寫S04為「已完成／未驗收」，列出仍需正式替換的角色、音效與VFX；未經使用者驗收不得進S05施工。

### 新增但尚未施工的S05規格與素材

- 完整規格：`docs/systems/tutorials/引氣入體第一段_感應靈氣_開發規格.md`。
- HUD／演出配置參考：`art/references/gameplay/sensing-spirit/sensing-spirit-gameplay-layout-v1.png`，只能作配置參考，圖內HUD與角色不可整張實裝。
- 無HUD背景來源：`art/references/gameplay/sensing-spirit/sensing-spirit-background-source-v1.png`。
- 本段是一次性完成度教學，不是25／30秒計分小遊戲；不得顯示時間、修為、連擊、剩餘次數、評級、Miss或失敗。
- 預定runtime路徑 `/assets/cultivation/sensing-spirit/sensing_spirit_bg.png` 尚未建立；背景、角色、頭髮、衣帶、前景竹、石台、靈氣粒子／流線與HUD必須分層。

已有 `C:\NTSI` 工作目錄時，從專案根目錄執行 `sync.bat`：

- `[1] Start work`：拉取遠端最新進度，再安裝／更新套件。
- `[2] Ending work`：建置、提交並推送本次工作。
- `[3] Status`：查看分支、未提交變更、Node 與套件狀態。
- `[4] Install dependencies only`：只處理套件，不再拉取。

新電腦第一次接手才使用：

```powershell
git clone https://github.com/index7777/NTSI.git
cd NTSI\game
npm ci
npm run dev
```

正式建置：

```powershell
cd NTSI\game
npm run build
```

完成一項任務後，先更新 `docs/production/HANDOFF.md`，再於專案根目錄執行 `sync.bat` 並選擇 `[2] Ending work`。

## 2026-07-22 雙電腦同步排雷紀錄

- 本次 `[1] Start work` 已成功拉取遠端，當時本機與 `origin/main` 同步於提交 `cfe5584 Sync NTSI project updates`。
- 舊版 `sync.bat` 直接在工作目錄執行；當 `git pull` 同時更新 `sync.bat` 本身時，CMD 會從變動後的檔案位移繼續讀取，曾出現 `'BRANCH" >nul 2>nul' 不是內部或外部命令`。現已改成先複製到 `%TEMP%` 再執行，pull 可安全更新根目錄腳本。
- 舊版在已有 `node_modules` 時仍執行 `npm ci`，會先移除套件；若 Vite 尚在背景執行，Windows 會鎖住 `node_modules/@esbuild/win32-x64/esbuild.exe`，造成 `EPERM unlink`。
- 本次鎖檔實際是本機殘留的 Vite 程序，不是另一台電腦：`cmd 4408 → node/vite 8040 → esbuild 44372`，服務位址為 `127.0.0.1:4174`。已只終止該 NTSI 程序樹。
- 新版同步工具在已有 `node_modules` 時改用 `npm install`；若仍出現 esbuild `EPERM`，先關閉 `dev.bat`、`npm run dev`、Vite 預覽或相關背景終端，再選 `[4]`，不要刪除原始碼或 Git 資料。
- 修正後已完成 `npm install`：新增12個套件、更新5個套件、0項漏洞；`npm run build` 成功。
- 截至本次交接，本機只有 `sync.bat` 因上述修正尚未提交；新任務開始後應保留此變更，完成工作時由 `[2] Ending work` 一併提交，禁止覆蓋或還原。

## 目前可玩範圍

- 首圖、存檔繼續與新的開始；「道心崩了」已完全取消，遊戲與機關均無此功能。
- 男女選角、輸入姓名、問仙石、靈根與四維資質。
- 入門修行、練氣一至九層、重修與失敗跌層。
- 柳如煙贈送筑基丹、裸衝／服丹、筑基一層。
- 筑基一至九層可修行、突破、失敗跌層，筑基九層圓滿後停在階段完成提示；雷劫原型暫不由正式流程進入。
- 歷史敘述（已過時）：舊版四個修行原型曾為滑切砍竹、水位平衡、限時藥園、記憶小周天。最新S04已用節奏砍竹、經脈時機心法與持續控制挑水覆蓋前三者中的相關舊玩法；不得從此句恢復舊實作。

## 最近完成

- 2026-07-23 S01首圖與選角最新正式配置母板：`art/references/ui/s01-character-selection/s01-character-selection-layout-approved-v1.png`。只有人物與場景烘在背景；中央標題裝飾、左右橫式角色選擇條、底部確認選擇均須依範例拆成獨立素材與程式文字。左上／右上只製作獨立、可跨場景重用的透明底金色圓環返回與透明底金色圓環設定齒輪；圓環內外都不得有黑底。圖示下方看似水墨的黑色橫向方塊不是圖片素材，改由CSS／Phaser程式繪製半透明矩形；返回／設定文字亦由程式渲染。左側資訊欄只使用獨立透明金色線框／頂部裝飾，底下黑色方形半透明層由CSS／Phaser程式繪製，文字亦由程式渲染，禁止金框與黑底烘成一張。允許重畫、去背或採授權素材，實機須盡量一模一樣。禁止整張母板實裝，亦禁止以現有紙卷或CSS膠囊代替。選角後漸隱並顯示中央姓名視窗；姓名視窗無「留下姓名」標題與分隔線，確認及返回需統一美術語言。
- 使用者指定所有未採用的場景圖保留在 `art/candidates`／預備區，不得因目前未實裝而刪除；乾淨選角背景候選亦保留供日後使用。
- S01取名正式配置母板：`art/references/ui/s01-name-entry/s01-name-entry-layout-approved-v1.png`。保留左上返回、右上設定與中央宣紙名冊構圖；移除輸入框上方「姓名」及左右分隔線，輸入框下方以程式即時顯示字數`N / 8`，不做右側重置鍵。確認姓名使用獨立金邊按鈕素材但不做外發光／光芒特效；所有文字與輸入功能由程式渲染。
- S01本輪退回修改已完成／未驗收：選角維持已烘入人物的正式橫式背景，只疊程式互動熱區與宣紙文字底板；右上功能列為「設定／修練記錄／成就」，姓名確認與返回使用同套宣紙按鈕。已通過1200×675、960×540、640×360實機檢查、四項驗證與正式build，瀏覽器錯誤為零；等待使用者驗收後才可改為已驗收。
- S01退回修改已完成／未驗收：主入口改用Kenney CC0透明光跡與火花粒子，向右移動並逐漸縮小淡出；完全移除「道心崩了」所有入口與刪檔程式，機關只保留音樂、畫面、關閉。

- 首圖已依指定概念圖重排：Logo左上且無印章、移除左下文字／印章、主按鈕下移，右下加入設定／修練記錄／成就三個同規格入口；成就顯示「尚未開放」。
- 首圖主狀態改為互斥：無有效存檔時顯示入宗者與「踏入仙門」；有有效存檔時不顯示入宗者且只顯示「繼續修行」，兩者共用相同按鈕錨點與尺寸。
- 最新覆蓋：完全移除「道心崩了」入口、場景卡、確認與刪檔流程；機關及遊戲內設定均不得出現。
- 修正首圖右側遮罩硬切造成的門樓右側接縫；遮罩改為透明起始的寬漸層。
- 首圖已加入分層雲霧、瀑布流動、水霧及光線動畫；水霧移除圓形程式假霧，改用 Kenney CC0 `smoke_01/02` 柔邊素材的低透明橫向擴散演出。
- 首圖功能列固定於右上，採 `art/references/ui/game-interface-style-v1.png` 母版的「水墨設色物件插畫」語言：透明資產直接落在場景上，不加卡片或底板，不得使用 Emoji、扁平 glyph、幾何 Phaser Graphics 或寫實 3D 道具。設定使用暖灰褐齒輪；修練記錄必須是帶封面紋章、書脊與紙邊的合冊日誌，不得使用翻開但無內容的素書；成就使用舊金褐功勳杯。圖示色相須避開場景草木的冷綠，文字需用暖米白配薄深墨邊，確保跨亮霧／深山背景可讀。4174 實機驗收零錯誤。
- 首圖已接入原生1200×675橫式背景與布局：Logo、無名入宗者、入口紙卷面板及「踏入仙途／繼續遊戲／新的開始」按鈕均改用橫式座標，不再落在舊960高畫布外。
- 4174實機測試已確認可從橫式首圖點擊進入選角，瀏覽器錯誤日誌為空；4173保留給使用者預覽。
- 修正首圖音樂切換時舊淡化計時器繼續修改已銷毀音軌的問題，同一音軌現在只保留一個fade timer。
- 確認全案改版順序：從首圖開始，依玩家實際流程逐頁重做原生1200×675橫式場景、UI／UX／HUD、插畫與動畫；完成至筑基九層圓滿，雷劫暫緩。
- 新增橫式首圖背景候選 `art/candidates/scenes/title-sect-approach-landscape-morning-v1.png`；尚待分層合成、按鈕安全區與實機驗收，不得標記正式。
- 取得 Kenney Particle Pack 1.1 並保留原始 CC0 授權於 `art/third-party/kenney-particle-pack/License.txt`；可用於商業遊戲，runtime 僅挑選實際使用素材，不整包複製。
- 問仙石頁面演出鎖定為獨立石頭／四種靈根狀態、授權光效、文字動畫、四維揭示、執事表情對話與前往雜役處按鈕的分鏡流程。
- 過時規則：四個日常修行小遊戲與吐納曾統一為30秒。最新明確覆蓋為砍竹30秒、參悟心法25秒、挑水25秒；S05感應靈氣不計時。其餘玩法時間尚須逐項依最新規格確認。
- 吐納計時已加入單次結算保護，避免30秒結束後重複觸發結果。
- 遊戲正式方向改為 1200×675、16:9 橫式；直式場景列為 legacy-portrait 參考，尚待全部重製。
- `art/references/ui/game-interface-style-v1.png` 定為 UI／UX／HUD 配置母板；`scene-card-transition-style-v1.png` 定為主場景、場景卡、小遊戲與結算流程母板。
- 新增根目錄 `AGENTS.md`、橫式 UI 規格、人物頭像規格與 UI 資產拆分清單；母板禁止整張實裝，功能與資產必須分層製作。
- 新增 ESC 系統選單、右下場景移動鎖定提示、入門日數與卯／午／酉時段存檔欄位。
- 靈根揭示文案、初期砍竹／心法限制、三段引氣入體與慕清璃吐納教學已開始改版。
- 小遊戲統一規格為 30 秒倒數；時間到未達成功條件判定失敗。
- 圖片生成目前暫停；現有活動插圖只作候選與構圖參考，尚未核准為正式場景卡。

- 角色畫風改為精緻水墨3.5～4頭身Q版。
- 建立新版角色同框比例驗收與拆圖重組P0規格。
- 首圖改為無名入宗者單人背影，不在首圖強調男女選角。
- 移除Logo閃電動畫；保留青玉雲紋呼吸。
- 加入三段音樂流程：Menu、Game Start、進入雜役處後的三首日常循環。
- 平時修行失誤仍依操作分數取得12～48修為。
- 筑基一至九層已接入修行、修為門檻、突破與失敗跌落流程；四維逐項獨立成長並受本層上限限制。
- 過時／暫緩紀錄：曾製作九重雷劫第一版原型與鏡頭速度感；目前不得由正式流程進入，也不得在筑基九層前置完成前擴充。

## 音樂流程

1. 首圖：`OST 001 NTSI menu`，首次音訊互動後漸入。
2. 選角、取名、問仙石與資質揭示：`OST 0001 NTSI game start`。
3. 點擊「前往雜役處」後：停止Game Start曲，三首日常音樂依序循環並漸入。

瀏覽器會限制未經互動的自動播放，因此Menu曲最早會在玩家首次點擊或按鍵後開始。

## 新版角色素材狀態

- `art/active/characters/player-male-master.png`：男玩家畫風基準。
- `art/candidates/characters/player-female-v2.png`：女玩家中性站姿候選，尚未完成正式拆圖重組驗收。
- `art/active/characters/liu-ruyan-v1.png`：柳如煙造型基準。
- `art/candidates/characters/mu-qingli-v2.png`：冷眼持劍修正版候選。
- `art/active/characters/sect-elder.png`：收徒執事。
- `art/candidates/characters/outer-sect-steward.png`：後續外門資源NPC，Demo暫不實裝。
- `art/reviews/character-scale-review-v1.png`：第一版同框驗收。
- `art/active/ui/title-traveler-v1.png`：首圖單人背影素材。

未通過原圖重組與同場景比例驗收的素材只能視為候選，不得標記正式完成。

## 素材搬移對照

第一階段圖片舊檔名與英文新路徑，統一查閱 docs/production/ASSET_MIGRATION_MAP.md。

## 強制人物美術與對話規範

凡涉及人物、服裝、表情、劇情對話或生成美術，動工前必須先讀取根目錄 `AGENTS.md` 與 `docs/art/CHARACTER_PORTRAIT_STANDARD.md`。角色資產狀態統一登錄於 `game/src/config/characters.ts`；沒有通過母板、比例及表情驗收的素材不得標記正式完成。

## 主要規格文件

- `gdd.md`
- `numbers.md`
- `breakthrough.md`
- `docs/narrative/遊戲開局演出與劇本_v1.md`
- `docs/art/角色美術與境界演出母板.md`
- `docs/systems/training-games/練氣修行小遊戲規格.md`
- `docs/production/PROJECT_MEMO.md`

只使用Markdown正式文件；不以舊TXT內容作為規格來源。

## 目前待辦

本節只保留里程碑摘要；精確順序、逐條使用者要求與狀態以 docs/production/WORK_QUEUE.md 為唯一依據，不得從舊清單自行挑項施工。

目前里程碑：先完成開局至筑基九層圓滿，以及2026-07-22交辦素材的整理、驗收與實際接入；九重雷劫暫緩，待前置內容完成後再依新概念製作。

1. 依橫式母板拆分 HUD、紙卷、木框、圖示、按鈕狀態與進度元件，不得整張使用母板。
2. 重做橫式首圖、選角、問仙石、宗門主庭院、洞天與築基洞府；核心場景需卯時、午時、酉時三版本。
3. 接入「宗門主庭院 → 場景卡 → 小遊戲 → 結算 → 時段推進」正式流程。
4. 完成外門壞執事交付《清心訣》劇情與人物／表情資產驗收。
5. 完成修行、渡劫、突破、跌境與遊玩時數統計介面。

- 待跨境服裝正式驗收後，補齊男女玩家各境界的活動劇情場景卡；卡片演出隨境界升級，小遊戲操作暫時一致。

1. 暫緩：雷劫原型、追蹤雷、小雷、第九劫、神識預判與回血丹，等使用者解除暫緩。
3. 驗收新版單人背影在實際首圖的大小、站位與按鈕遮擋。
4. 完成玩家正式拆圖、重組疊合與數值驗收。
5. 製作首圖前景草、雲海、宗門光與景深分層；不使用閃電動畫。
6. 重製問仙石場景角色與宣紙資質UI。
7. 為四個修行小遊戲補齊水墨背景、角色動作與互動素材。
8. 實機測試觸控滑切、挑水平衡、周天描線與雷劫QTE。

## 驗證要求

- 每次修改程式後執行 `npm run build`。
- 角色同場景必須輸出比例驗收截圖。
- 拆圖必須重組比對原圖；沒有比對不算合格。
- 手機以至少360px寬畫面驗證，網頁同步驗證滑鼠操作。
- 每次完成使用者指派工作後更新本文件，再執行同步。


## 2026-07-24 問仙石游標追加

- 問仙石互動開放後，指標滑入石頭範圍即隱藏系統游標並顯示專用手部素材；離開石頭範圍才隱藏手部。手部執行尺寸已縮為原先的 50%（54×79），按住與放開期間沿用同一素材。
- 長按階段原本單獨覆蓋在「問仙石」文字上的發光碑文層已移除。蓄力改由石頭中心淡金核心呈現；完成時依序播放單靈根抽卡式金白放射爆光、金色閃白與全畫面金雲霧光，再切入靈根揭示。
- 正式素材：`game/public/assets/vfx/question-stone/single-root-golden-burst-v1.png`、`game/public/assets/vfx/question-stone/single-root-golden-mist-v1.png`；原稿與處理檔分別位於 `art/raw/vfx/question-stone/`、`art/processed/vfx/question-stone/`。
# 2026-07-24 砍竹 HUD 最新交接

- 砍竹圓形判定槽已改為 `CircularChargeGauge` 程式繪製，不使用固定圖片序列。槽體為透明中心、灰青半透明空槽、淡金順時針進度、細金外框與低強度柔光；進度由既有 `RhythmTimingController.snapshot().cycleProgress` 自動循環，完成停留後平滑歸零，暫停時凍結。
- 右側連擊數字為獨立 DOM/CSS 元件 `game/src/minigames/shared/ComboCounter.ts`，數值只讀既有 `controller.stats.combo`；「連擊」中文字使用 `combo-calligraphy-gold-v1.png`，下層使用 `combo-ink-backing-v1.png`，三者保持獨立分層。0 時整組隱藏，增加時數字播放 220ms 短促上浮縮放，10／25 以上採克制的淡金分級，暫停不重置。
- 左側竹子資訊的「超額完成」固定另起一行，避免數字變長時橫向超出資訊卡。
- 砍竹結算已套共用 `shared-result-panel-v1.png`，只有「修行完成」置中；其餘標籤靠左、所有數字固定同一起始線。八列內容依序淡入，底部為「再次修行／離開」兩顆 HUD 按鈕。
# NPC 生成與方向處理強制規則

任何新對話、新模型在生成、編修或接入 NPC 資產前，必須先完整閱讀：

- `docs/art/NPC_MODULAR_CHARACTER_STANDARD.md`
- `assets-spec/characters.yaml`

NPC 只允許使用成年男、成年女、孩童三種共用身材模組。建立人物差異時，只能替換頭部、髮型、服裝與配飾，不得為單一 NPC 重新自由設計身材比例。

角色需要改變面向時，只能在 Phaser、CSS 或其他執行環境中水平反轉既有角色圖，不得為左右方向重新生成。角色圖、NPC 對話底板與文字必須分層；反轉只作用於角色圖，對話框和文字不得被鏡像。

所有 NPC 生成提詞必須由 `assets-spec/characters.yaml` 的固定欄位組裝，不得自由發揮。至少鎖定身材模組、角色識別、頭部、髮型、服裝、配飾、表情、姿勢、鏡頭、光源、透明背景、角色一致性與限制條件。
